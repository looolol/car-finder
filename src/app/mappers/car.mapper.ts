/*import {Car} from "../models/car.model";
import {AutoDevApiCar} from "../models/auto-dev.api-car.model";


export class CarMapper {

  translate(apiCar: AutoDevApiCar): Car {
    const car: Car = {};

    if (apiCar.make !== null) car.make = apiCar.make;
    if (apiCar.model !== null) car.model = apiCar.model;
    if (apiCar.year !== null) car.year = apiCar.year;

    return car;
  }

}
*/
